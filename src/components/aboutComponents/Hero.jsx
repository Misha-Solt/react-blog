import styles from './hero.module.css'

const Hero = () => {
  return (
    <div id="hero" className={styles.heroBlock}>
      <h3 className={styles.heroText}>Hero</h3>
    </div>
  )
}

export default Hero
